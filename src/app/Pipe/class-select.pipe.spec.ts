import { ClassSelectPipe } from './class-select.pipe';

describe('ClassSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
